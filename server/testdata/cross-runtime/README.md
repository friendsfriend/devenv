# Cross-runtime endpoint fixtures

Fixtures describe supported endpoint boundaries without requiring a running provider in unit tests. Integration runners may materialize each manifest with unique project, cluster, namespace, release, and port identities.
