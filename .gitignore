# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.bun-build

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env
.DS_Store

# Build output
dist/
logs/
devenv
server/tui/
tui/server-binaries/

# project folders
devenv-apps/*
devenv-apps

# TypeScript/Bun frontend (tui)
node_modules/
tui/node_modules/
tui/packages/*/node_modules/
tui/.bun/
tui/.bun-build/
tui/dist/
tui/server-binaries/
tui/packages/*/dist/
